#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,n,cnt;
    cnt=1;
    scanf("%d",&n);
    int data[n];sub[n-1];

    for(i=0;i<=n-1;i++)
    {
        scanf("%d",&data[i]);
    }
    for(i=0;i<=n-2;i++)
    {
        sub[i]=data[i+1]-data[i];
        if(sub[i]==0)
            {
                if()
            }

    }





    return 0;
}
