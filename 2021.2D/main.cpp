#include <bits/stdc++.h>

using namespace std;
class Solution
{
public:
    vector<vector<int>> color(C,vector<int>(R));
    vector<vector<int>> height(C,vector<int>(R));


};

int main()
{
    int R,C,Q;
    cin>>R>>C>>Q;

    return 0;
}
